{% extends "base.twig" %}

{% block content %}

	<section class="section">
		{% for post in posts %}
			<div class="row">
				<div class="columns">
					{# start here #}
				</div>
			</div>
		{% endfor %}
	</section>

{% endblock %}
